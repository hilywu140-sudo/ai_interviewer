# ==================== 数据库配置 ====================
# 部署脚本会自动创建数据库，密码请修改为安全的值
DATABASE_URL=postgresql://ai_user:CHANGE_THIS_PASSWORD@localhost:5432/ai_interviewer

# ==================== LLM API 配置 ====================
# DeepSeek（推荐用于开发测试，成本低）
DEEPSEEK_API_KEY=sk-your_deepseek_api_key_here
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEFAULT_LLM_PROVIDER=deepseek

# OpenAI（可选，生产环境）
# OPENAI_API_KEY=sk-your_openai_api_key_here

# Anthropic Claude（可选，生产环境）
# ANTHROPIC_API_KEY=sk-ant-your_anthropic_api_key_here

# ==================== 阿里云服务配置 ====================
# DashScope（语音识别 + 通义千问）
DASHSCOPE_API_KEY=sk-your_dashscope_api_key_here

# OSS 对象存储（音频文件上传）
ALIYUN_ACCESS_KEY_ID=your_aliyun_access_key_id
ALIYUN_ACCESS_KEY_SECRET=your_aliyun_access_key_secret
ALIYUN_OSS_BUCKET=your_oss_bucket_name
ALIYUN_OSS_ENDPOINT=oss-cn-shanghai.aliyuncs.com

# 阿里云短信（可选，用于手机号登录）
# ALIYUN_SMS_SIGN_NAME=your_sms_sign_name
# ALIYUN_SMS_TEMPLATE_CODE=your_sms_template_code

# ==================== 应用配置 ====================
APP_ENV=production

# 安全密钥（至少32字符，可用 openssl rand -hex 32 生成）
SECRET_KEY=CHANGE_THIS_TO_A_SECURE_RANDOM_STRING

# JWT 认证
JWT_SECRET_KEY=CHANGE_THIS_TO_ANOTHER_SECURE_STRING
JWT_ALGORITHM=HS256
JWT_EXPIRE_DAYS=7

# ==================== CORS 跨域配置 ====================
# 部署后修改为实际的 Vercel 域名和 Cloudflare Tunnel 域名
CORS_ORIGINS=https://your-app.vercel.app,https://xxx-xxx-xxx.trycloudflare.com

# ==================== LangSmith 监控（可选）====================
# LANGSMITH_API_KEY=lsv2_pt_your_langsmith_api_key_here
# LANGSMITH_PROJECT=ai-interviewer
# LANGSMITH_TRACING=true
