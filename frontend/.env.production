# 生产环境配置
# 部署到 Vercel 时使用（在 Vercel 控制台配置环境变量）

# 后端 API 地址（替换为你的阿里云服务器域名）
NEXT_PUBLIC_API_URL=https://api.yourdomain.com

# WebSocket 地址（替换为你的阿里云服务器域名，使用 wss:// 协议）
NEXT_PUBLIC_WS_URL=wss://api.yourdomain.com
